import Home from '../src/modules/Home';
import { shallow, render } from 'preact-render-spy';


describe('Home component tests', () => {
	test('Home renders chart', () => {
		const context = render(<Home />);
  });
});
